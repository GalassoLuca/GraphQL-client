# GraphQL-client
GraphQL client in order to manage the query properly with the object

https://github.com/GalassoLuca/GraphQL-server

# Static index
A simple example to do query

# Dynamic index
A dynamic approach in order to handle multiple queries in a cleaner way
